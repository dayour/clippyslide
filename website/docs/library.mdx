---
id: library
title: Deck Library
sidebar_label: Library
sidebar_position: 3
description: Every ClippySlide deck as a single self-contained HTML presentation.
---

import useBaseUrl from '@docusaurus/useBaseUrl';

Every deck below is a **single self-contained HTML file**. All stylesheets,
scripts, and images are inlined, so a deck opens from this site, from disk, or
from an email attachment with no other files and no network access.

<div className="deckGrid">
  <a class="deckCard" href={useBaseUrl('/decks/coe-framework-clippydeck.html')}>
    <span className="deckBadge">TileSlide</span>
    <span className="deckName">Copilot CoE Framework</span>
    <span className="deckBlurb">Center of Excellence operating model, from intake and governance through measurement.</span>
    <span className="deckMeta">24 slides</span>
  </a>
  <a class="deckCard" href={useBaseUrl('/decks/clippyflow-brand-clippydeck.html')}>
    <span className="deckBadge">ClippyFlow</span>
    <span className="deckName">ClippyFlow Brand Deck</span>
    <span className="deckBlurb">Brand manifesto, spectrum palette, the Nitrous Rule, components, and usage guardrails.</span>
    <span className="deckMeta">11 slides</span>
  </a>
  <a class="deckCard" href={useBaseUrl('/decks/task-planning-clippydeck.html')}>
    <span className="deckBadge">ClippyFlow</span>
    <span className="deckName">Task Planning Flow Architecture</span>
    <span className="deckBlurb">Generic planning paths from intake to an approved plan, with stable tile and card identifiers.</span>
    <span className="deckMeta">8 slides</span>
  </a>
  <a class="deckCard" href={useBaseUrl('/decks/atlassian-connectors-clippydeck.html')}>
    <span className="deckBadge">Pine</span>
    <span className="deckName">Atlassian Connectors</span>
    <span className="deckBlurb">Connector landscape and integration patterns for Jira and Confluence.</span>
    <span className="deckMeta">7 slides</span>
  </a>
  <a class="deckCard" href={useBaseUrl('/decks/clippyflow-samples-clippydeck.html')}>
    <span className="deckBadge">ClippyFlow</span>
    <span className="deckName">ClippyFlow Sample Slides</span>
    <span className="deckBlurb">Reference ClippyFlow slides: title, orchestration, and the spectrum board.</span>
    <span className="deckMeta">3 slides</span>
  </a>
  <a class="deckCard" href={useBaseUrl('/decks/tileslide-samples-clippydeck.html')}>
    <span className="deckBadge">TileSlide</span>
    <span className="deckName">TileSlide Sample Slides</span>
    <span className="deckBlurb">Legacy TileSlide reference slides used as layout examples for new decks.</span>
    <span className="deckMeta">3 slides</span>
  </a>
</div>

## Controls

| Key | Action |
| --- | --- |
| `←` `→` `PageUp` `PageDown` `Space` | Previous / next slide |
| `Home` `End` | First / last slide |
| `F` | Fullscreen |
| `L` | Open this library from inside any deck |
| `H` | Pin the control bar |

The control bar hides itself and returns on pointer movement. Clicking the left
or right edge of the stage also navigates.

## Building

```bash
node generator/build-clippydecks.js
```

This packages every deck folder, copies the results into
`website/static/decks/`, regenerates the ClippyFlow `planner-card-math`
slides, and rewrites this page.

To add a deck, append an entry to the `DECKS` registry at the top of
[`generator/build-clippydecks.js`](https://github.com/dayour/clippyslide/blob/main/generator/build-clippydecks.js)
and rebuild. The registry drives the in-deck Library overlay and this page at
once.

---

*6 decks · 56 slides · generated by `generator/build-clippydecks.js`.*
